apiVersion: 1

providers:
  - name: 'gitopia-dashboards'
    orgId: 1
    folder: 'Gitopia Storage'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
